;; User info
(setq user-full-name "Rain"
      user-mail-address "kitgxrl@protonmail.com")

;; Themeing
(setq doom-theme 'doom-nord)
(setq doom-font "Iosevka Nerd Font-13")

;; Org shit ill never use probably lol
(setq org-directory "~/org/")

;; Line numbers
(setq display-line-numbers-type t)
